<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/border"
    android:layout_gravity="center"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/add_picture_item"
        android:layout_width="@dimen/detail_picture_size"
        android:layout_height="wrap_content"
        android:maxHeight="@dimen/detail_picture_size"
        android:layout_margin="@dimen/border_width"
        android:src="@drawable/ic_no_image_available"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/new_picture_remove_picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="mini"
        app:layout_anchor="@id/add_picture_item"
        app:layout_anchorGravity="bottom|end"
        android:src="@drawable/ic_close_black_24dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>