<resources>
    <!-- Base application theme. -->
    <style name="Theme.RealEstateManager" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:actionBarStyle">@style/ActionBarStyle</item>
    </style>

    <style name="ActionBarStyle" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" >
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <style name="LoginTheme" parent="Theme.RealEstateManager">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="windowNoTitle"> true </item>
    </style>

    <!-- Map View -->
    <style name="MapViewMessageTitle" >
        <item name="android:textColor"> @color/black </item>
        <item name="android:textStyle"> bold </item>
    </style>
    <style name="MapViewMessage" >
        <item name="android:textColor"> @color/black </item>
    </style>

    <!-- Detail -->
    <style name="DetailTitleCategory">
        <item name="android:textColor"> @color/colorSecondary</item>
    </style>

    <!-- Setting -->
    <style name="PreferenceCategory" parent="Theme.RealEstateManager">
        <item name="colorAccent"> @color/colorSecondary </item>
    </style>


</resources>